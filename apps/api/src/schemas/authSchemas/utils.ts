const DATE_FORMAT = "YYYY/MM/DD"
const PHONE_PATTERN = /^\(?(\d{2})\) ?([9]{1})?(\d{4})-?(\d{4})$/

const BAREAR_TOKEN_PATTERN = /^Bearer\s[\w-]*\.[\w-]*\.[\w-]*$/

export { DATE_FORMAT, PHONE_PATTERN, BAREAR_TOKEN_PATTERN }
